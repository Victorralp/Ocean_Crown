# Ocean Crown

An eCommerce starter with Paystack payments and Brevo SMTP email integration.

Setup
1. Fill `backend/.env.example` -> `backend/.env`
2. In `backend`: npm install && npm run dev
3. In `frontend`: npm install && npm run dev
